1.swit_to_alter方法报错无法获取到弹窗错误提示语: 已解决，抛弃alert方法通过获取class，定位到属性然后在获取值
2.截图保存图片名称，分隔符中不能出现：，否则会报错
3.ddt字典数据key值会被做为用例的标识，所以数据封装外层需要多来一层，，死铺盖
4.run.py中setup方法存在问题，当前实例化logon方法，导致用例无法动态获取其他页面方法
5.time.sleep后续需更换为显示还是隐式的等待方法
6.用例操作未进行断言判断成功与否
7.元素中空格要用英文逗号替换否则找不到
8.未添加任何异常
9.代码狗屎一样，逻辑乱一点点儿都没有面向对象 都是面向过程
10.sql语句执行时，传入中文字符串需额外增加一个双引号，否则会报错
11.sql语句添加模糊查询时报语法错误(存在转义）
12.sql count函数统计存在问题
13.sql查询条件值需优化，当前只能查询单个值，需动态获取多个值--已完成通过*args传入tuple
14.隐士等待只针对find_element方法，操作类不生效，显示等待是给浏览器对象设置一个等待时间
15.entermoudle模块中树菜单下标后期考虑将数据写入文件通过文件中进行动态获取
16.操作栏名称对应元素暂无法获取
17.数据返回存在null数据，（原因未知）通过推导式过滤空格重新写入新的列表